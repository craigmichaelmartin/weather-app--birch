define(function (require) {
    'use strict';

    var DayView = require('components/day');

    describe('Day view', function () {

        it('should be defined', function () {
            expect(DayView).not.to.be.undefined;
        });

    });
});
