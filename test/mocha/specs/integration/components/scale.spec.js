define(function (require) {
    'use strict';

    var ScaleView = require('components/scale');

    describe('Scale view', function () {

        it('should be defined', function () {
            expect(ScaleView).not.to.be.undefined;
        });

    });
});
